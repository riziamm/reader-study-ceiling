# real ratings are not released; the raw export also contains reader emails
data/real/

# synthetic data is regenerated by `make synthetic`
data/synthetic/*.csv

# per-replicate sweep files (large) and checkpoints
outputs/tables/_raw/
outputs/tables/_ckpt_*.csv

.veRSC/
.venv/
__pycache__/
*.pyc
